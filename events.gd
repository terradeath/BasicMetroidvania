extends Node

signal add_screenshake(amount, duration)
signal door_entered(door)
signal level_changed()
signal camera_limits_changed(left, right, top, bottom)
signal boss_triggered(level)
signal player_died()
